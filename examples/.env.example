# Example .env.example — this is what envguard checks your .env against.
# Inline hints (optional): add "# pattern:REGEX" to validate the value's shape.

DATABASE_URL=postgres://user:pass@localhost:5432/mydb # pattern:^postgres://
PORT=3000 # pattern:^[0-9]+$
STRIPE_SECRET_KEY=
NODE_ENV=development
